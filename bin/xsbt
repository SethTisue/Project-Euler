#!/bin/sh

LAUNCH=`dirname $0`/sbt-launch-0.11.0-RC1.jar
URL='http://repo.typesafe.com/typesafe/ivy-snapshots/org.scala-tools.sbt/sbt-launch/0.11.0-RC1/sbt-launch.jar'

if [ ! -f $LAUNCH ] ; then
  curl -f $URL -o $LAUNCH || exit
fi

java \
  -Xmx1536M \
  -XX:MaxPermSize=256M \
  $JAVA_OPTS \
  -classpath $LAUNCH \
  xsbt.boot.Boot "$@"
